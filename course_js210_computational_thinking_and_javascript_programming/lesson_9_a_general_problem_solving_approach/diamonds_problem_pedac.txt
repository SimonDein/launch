Given a number, generate a diamond where the given number will be the width
Example:

Given 3:
 *
***
 *

 Given 5:
  *
 ***
*****
 ***
  *

Understand the Problem:
Input:
  Integer
  Must be positive
  Must be odd

Output:
  A diamond / start printed to the console

Mental Model:
  Given 5
  We want an array of spaces: [2, 1, 0, 1, 2]
  And an array of stars: [1, 3, 5, 3, 1]

Examples / Test Cases:
 1 =>
 *
 
 3 =>
  *
 ***
  *
 
 0 =>
 undefined
 
 -1 =>
 undefined
 
 4 =>
 undefined

Datastructure:
  We can use a for loop to generate two arrays - one containing the spaces, another one containing the number of stars.


Algorithm:
  If N is not a positive odd integer
    Return undefined

  Set rowStars = 1;
  Set increment = 2
  Set countOfSpaces = 1;
  Set spaces = []
  Set stars = []

  for(; stars >= 1; )
    spaces.push(N - rowStars / 2);
    stars.push(rowStars)

    if rowStars === N Set increment = -2
    rowStars += increment;

print that shit out